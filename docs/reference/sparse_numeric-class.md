# Sparse numeric vector class

An S4 class for representing numeric vectors in sparse form.

## Slots

- `value`:

  Numeric vector of non-zero values.

- `pos`:

  Integer vector of positions of the non-zero values.

- `length`:

  Single integer giving the length of the full vector.
